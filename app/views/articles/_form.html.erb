<div class="container col-md-5">
  <div class="card">
    <div class="card-header">
      <h4>Formulario de Articulo</h4>
   </div>
  <%= simple_form_for @article, validate: true do |f| %>
    <%= f.error_notification %>
    <div class="card-body">
      <%= f.input :title, label: "Titulo", input_html: { class: "form-control" } %>
      <%= f.input :description,label: "Descripción",as: :text, input_html: { class: "form-control",rows: 6 }, hint: 'máximo 300 caracteres'  %>
      <br>
      <%= f.label :regions, label: "Región" %>
      <select class="form-control" id="regions"></select>
      <small class="mb-2 form-text text-muted">región-provincia</small>
      <%= f.input :district_id ,label: "Comuna", required: true,as: :select , collection: Article.get_district(f.object.id) || [], 
        prompt: "Seleccione Comuna", input_html: { class: "form-control mb-3" } %>
      <% action_art = controller.action_name == "edit" ? "Editar" : "Crear" %>
      <%= f.button :submit,"#{action_art} Articulo",class: 'btn-block mb-4 btn btn-sm btn-primary' %>
  <% end %>
  </div>
</div>
